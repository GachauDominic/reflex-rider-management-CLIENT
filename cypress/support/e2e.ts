// Cypress support file — intentionally minimal. Every spec stubs the
// Reflex API directly with cy.intercept() rather than relying on a
// running backend/seeded database, so no global fixtures live here.
